import { ipcRenderer, IpcRendererEvent } from "electron";
import fs from "fs";

const listdir = (event, dirpath) => {
    // TODO: 
}

const getFileInfo = (event, filepath) => {
    // TODO:
}

export default { listdir, getFileInfo };
